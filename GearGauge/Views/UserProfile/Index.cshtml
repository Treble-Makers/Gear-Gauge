 @* @model GearGauge.ViewModels.UpdateUserProfileViewModel 

    <h1>User Profile</h1>

   <div>
    <form asp-action="UserProfile" method="post" enctype="multipart/form-data">
    <div class="form-group">
            <label asp-for="UserName">UserName</label>
            <input asp-for="UserName" class="form-control" />
            <span asp-asp-validation-for="UserName" class="text-muted"></span>
            // text-muted vs text- danger 
        </div>
        <div class="form-group">
            <label asp-for="ContactEmail">Email</label>
            <input asp-for="ContactEmail" class="form-control" />
            <span asp-asp-validation-for="ContactEmail"
            class="text-muted"></span>
        </div>
         <div class="form-group">
            <label asp-for="Address">Address</label>
            <input asp-for="Address" class="form-control" />
            <span asp-asp-validation-for="Address" class="text-muted"></span>
        </div>
        <div class="form-group">
            <textarea rows="10" cols="50" placeholder="About Me"></textarea>
            <span asp-asp-validation-for="About Me"
            class="text-muted"></span>
        </div>
        <div class="form-group">
        <label asp-for="ProfilePicture"></label>
        <input asp-for="ProfilePicture" type="file" class="form-control" />
        <span asp-validation-for="ProfilePicture" class="text-danger"></span>
        </div>
        <div>
            <button type="submit" class="btn btn-success">Update</button>
        </div>
    </form>
   </div>
   <div>

   
 </div>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
} 

     @if (ViewBag.MessageBody != null)
        {
            <div>@ViewBag.MessageBody</div>
        } 
        // this last part of code is from contact-us form  *@
         