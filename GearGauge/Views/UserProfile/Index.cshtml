
@*@model GearGauge.ViewModels.UserProfileViewModel

@{
    ViewData["Title"] = "User Profile";
}

<h2>@ViewData["Title"]</h2>

<div id="profile-container">
    @if (Model.IsEditMode)
    {
        <form asp-action="Edit" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserName" class="control-label"></label>
                <input asp-for="UserName" class="form-control" />
                <span asp-validation-for="UserName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Email" class="control-label"></label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Address" class="control-label"></label>
                <input asp-for="Address" class="form-control" />
                <span asp-validation-for="Address" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AboutMe" class="control-label"></label>
                <textarea asp-for="AboutMe" class="form-control" rows="5"></textarea>
                <span asp-validation-for="AboutMe" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ProfilePicture" class="control-label"></label>
                <input asp-for="ProfilePicture" type="file" class="form-control" accept="image/*" />
                <span asp-validation-for="ProfilePicture" class="text-danger"></span>
            </div>
            @if (!string.IsNullOrEmpty(Model.CurrentProfilePictureUrl))
            {
                <div class="form-group">
                    <img src="@Model.CurrentProfilePictureUrl" alt="Current Profile Picture" style="max-width: 200px;" />
                </div>
            }
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    }
    else
    {
        <dl class="row">
            <dt class="col-sm-2">Username:</dt>
            <dd class="col-sm-10">@Model.UserName</dd>
            <dt class="col-sm-2">Email:</dt>
            <dd class="col-sm-10">@Model.Email</dd>
            <dt class="col-sm-2">Name:</dt>
            <dd class="col-sm-10">@Model.Name</dd>
            <dt class="col-sm-2">Address:</dt>
            <dd class="col-sm-10">@Model.Address</dd>
            <dt class="col-sm-2">About Me:</dt>
            <dd class="col-sm-10">@Model.AboutMe</dd>
        </dl>
        @if (!string.IsNullOrEmpty(Model.CurrentProfilePictureUrl))
        {
            <div class="form-group">
                <img src="@Model.CurrentProfilePictureUrl" alt="Profile Picture" style="max-width: 200px;" />
            </div>
        }
        <div class="form-group">
            <a asp-action="Index" asp-route-isEditMode="true" class="btn btn-primary">Edit</a>
            <form asp-action="Delete" method="post" style="display: inline;">
                <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete your profile?')">Delete Profile</button>
            </form>
        </div>
    }
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}



*@  
@model GearGauge.ViewModels.UserProfileViewModel

@{
    ViewData["Title"] = "User Profile";
}

<h2>@ViewData["Title"]</h2>

<div>
    <dl class="row">
        <dt class="col-sm-2">Username:</dt>
        <dd class="col-sm-10">@Model.UserName</dd>
        <dt class="col-sm-2">Email:</dt>
        <dd class="col-sm-10">@Model.Email</dd>
        <dt class="col-sm-2">Name:</dt>
        <dd class="col-sm-10">@Model.Name</dd>
        <dt class="col-sm-2">Address:</dt>
        <dd class="col-sm-10">@Model.Address</dd>
        <dt class="col-sm-2">About Me:</dt>
        <dd class="col-sm-10">@Model.AboutMe</dd>
    </dl>
    @if (!string.IsNullOrEmpty(Model.CurrentProfilePictureUrl))
    {
        <div class="form-group">
            <img src="@Model.CurrentProfilePictureUrl" alt="Profile Picture" style="max-width: 200px;" />
        </div>
    }
</div>

<div>
    <a asp-action="Edit" class="btn btn-primary">Edit Profile</a>
    <form asp-action="Delete" method="post" style="display: inline;">
        <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete your profile?')">Delete Profile</button>
    </form>
</div>