@using GearGauge.Models
@model List<MusicItem>

    <h1>Gear Gauge</h1>

    <p>
        <a asp-controller="MusicItem" asp-action="Add">Add MusicItems</a>
    </p>

    @if(Model.Count == 0)
    {
        <p>No Music Items yet!</p>
    }
    else
    {
        <table class="table">
            <tr>
                <th>
                    Title
                </th>
                <th>
                    Description
                </th>
                <th>
                    MarketValue
                </th>
                <th>
                    HaveOne
                </th>
                <th>
                    WantOne
                </th>
                <th>
                    Description
                </th>
            </tr>
            @foreach (var evt in Model)
            {
                <tr>
                    <td>@evt.MusicItems</td>
                    <td><a asp-controller="MusicItem" asp-action="Detail" asp-route-id="@evt.Id">@evt.Title</a></td>
                    <td>@evt.Title</td>
                    <td>@evt.Description</td>
                    <td>@evt.MarketValue</td>
                    <td>@evt.HaveOne</td>
                    <td>@evt.WantOne</td>
                </tr>
            }
        </table>

    }
