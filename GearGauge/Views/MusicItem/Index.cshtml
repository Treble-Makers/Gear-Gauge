@using GearGauge.Models
@model List<MusicItem>

    <h1>Gear Gauge</h1>

    <p>
        <a asp-controller="Music Items" asp-action="Add Music Item">Add MusicItems</a>
    </p>

    @if(Model.Count == 0)
    {
        <p>No Music Items yet!</p>
    }
    else
    {
        <table class="table">
            <tr>
                <th>
                    Title
                </th>
                <th>
                    Description
                </th>
            </tr>
            @foreach (var evt in Model)
            {
                <tr>
                    <td>@evt.MusicItems</td>
                    <td><a asp-controller="MusicItems" asp-action="Detail" asp-route-id="@evt.Id">@evt.Title</a></td>
                    <td>@evt.Title</td>
                    <td>@evt.Description</td>
                </tr>
            }
        </table>

    }
