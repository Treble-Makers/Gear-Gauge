@using GearGauge.ViewModels
@model MusicItem


<h1>Delete Gear Inventory</h1>

<form method="post">
    @foreach (var evt in ViewBag.MusicItems)
    {
        <div class="form-group">
            <label>
                <span>@evt.Title</span>
                <input type="checkbox" name="musicItemIds" value="@evt.Id" />
            </label>
        </div>
    }

        <input type="submit" value="Delete Selected Gear Inventory" class="btn btn-danger" />
</form>